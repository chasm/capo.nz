---
import "./index.css"

const { pathname } = Astro.url

const isHome = pathname.length < 2
---

{
	isHome ? (
		<div class="logo">capo.</div>
	) : (
		<div class="logo">
			<a
				href="/"
				title="To the home page."
			>
				capo.
			</a>
		</div>
	)
}
